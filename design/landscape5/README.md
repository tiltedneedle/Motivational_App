# Landscape v5 (9 Sept 2026)
The first Morrow direction (Landscape, v1–v4, archived untouched at `archive/v4-landscape/`) rebuilt for the self-authoring product.
The idea that changed: the sun rises when the user writes. The Fifteen raises the sun over fifteen minutes; each goal on Today is a sun-arc filled to its fraction; the seal ring fills when you hold.
Three voices in type: Newsreader italic (the future self), Newsreader roman (only the user's own words), Instrument Sans (interface, coach), Geist Mono (readouts).
- Generate: `python gen.py` (17 artboards), then seed with the design skill helper to `../morrow-landscape-v5.html`.
- Studio (`../studio/`) remains the current direction; this is the alternate.
