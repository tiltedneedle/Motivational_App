node_modules/
dist/
.expo/
web-build/
.env
.env.local
*.log
.playwright/
test-results/
coverage/

# Written by scripts/e2e.mjs when a run fails, overwritten on the next one.
# A debugging artefact, not a file anybody reviews: committed, it turned every
# failed run into a binary diff in the next commit.
e2e-failure.png

# Generated by `expo prebuild`. This project uses continuous native generation:
# the native folders are output, not source, and app.json is where the native
# configuration actually lives. Committing them means editing two places and
# forgetting one.
apps/mobile/android/
apps/mobile/ios/
scripts/shots/
supabase/.temp/
