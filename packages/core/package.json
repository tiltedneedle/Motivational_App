{
  "name": "@morrow/core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:eval": "vitest run --config vitest.eval.config.ts",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^4.6.0"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vitest": "^3.2.7"
  }
}
