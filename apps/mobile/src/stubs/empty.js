// An empty module. On the web, metro.config.js resolves react-native-reanimated
// here: nothing in the app uses it, and gesture-handler falls back to its own
// implementation when it finds no `useSharedValue`.
module.exports = {};
